<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ordre Bekræftet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .confirmation-container {
            background-color: #fff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            width: 400px;
        }
    </style>
</head>
<body>
    <div class="confirmation-container">
        <h1>Ordre Bekræftet</h1>
        <p><strong>Navn:</strong> @Model.FirstName</p>
        <p><strong>Efternavn:</strong> @Model.LastName</p>
        <p><strong>E-mail:</strong> @Model.Email</p>
        <p><strong>Telefonnummer:</strong> @Model.PhoneNumber</p>
        <p><strong>Adresse:</strong> @Model.Address</p>
        <p><strong>Postnummer:</strong> @Model.ZipCode</p>
        <p><strong>By:</strong> @Model.City</p>
     
    </div>
</body>
</html>